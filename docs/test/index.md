# Test reports

Reports are generated by `python -m tools.pipeline --latest --docs` after each test run.

- Unit test reports are stored under `unit/`.
- Continuous test reports are grouped by category under `ct/`.

